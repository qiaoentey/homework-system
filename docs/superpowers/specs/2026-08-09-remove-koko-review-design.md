# 取消 KOKO 与“复”点名设计

- 点名栏只保留：到、缺席、冲、餐、功、补。
- 名单上方取消 KOKO 统计卡，使页面与可操作项目一致。
- Express 与 Sites Worker 拒绝新的 `koko`、`review` 点名写入。
- 数据库中已有的历史记录不删除，其他出席与缺席统计不改变。
- 最新版本包含先前待发布的名单、登录、留堂、特别备注、洗澡及取消“回”等全部更新，并在验证后公开发布。
