name: Homework checker Pages

on:
  workflow_run:
    workflows: ["Homework checker CI"]
    types: [completed]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: homework-checker-pages
  cancel-in-progress: true

jobs:
  deploy:
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main'
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    defaults:
      run:
        working-directory: homework-checker
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: homework-checker/package-lock.json
      - run: python -m pip install -r requirements-answer-pdf.txt
      - run: npm ci
      - run: npm run test:answers
      - run: npm run verify:answers
      - run: npm test
      - run: npm run build:pages
      - run: npm run verify:pages
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v4
        with:
          path: homework-checker/dist
      - uses: actions/deploy-pages@v4
        id: deployment
